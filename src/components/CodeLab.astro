---
import { readFileSync } from 'node:fs';
interface Props {
  id: string;
  file: string;
  exercise: string;
  platform: string;
}
const { id, file, exercise, platform } = Astro.props;
const source = readFileSync(file, 'utf8');
---

<section class="code-lab" id="code-lab" aria-labelledby="code-lab-title" data-code-id={id}>
  <p class="eyebrow">
    CODE LAB <span>/ C++20</span>
  </p>
  <h2 id="code-lab-title">动手验证，再改变一个条件</h2>
  <p>{exercise}</p>
  <p class="muted">
    {platform === 'linux'
      ? '需要 Linux 系统调用；在线沙箱可能不支持，请用 Linux / WSL 作为本地替代。'
      : '示例包含正确性断言。在线机器的计时不能用作性能基准。'}
  </p>
  <label for="code-editor">
    C++ 编辑器 <span class="muted">· 自动保存到此浏览器</span>
  </label>
  <textarea
    id="code-editor"
    rows="20"
    spellcheck="false"
    autocomplete="off"
    maxlength="100000"
  >{source}</textarea>
  <details>
    <summary>原始示例（用于对照）</summary>
    <pre id="original-code">{source}</pre>
  </details>
  <div class="code-actions">
    <button class="primary-button" type="button" id="run-code">
      发送到 Compiler Explorer 并运行
    </button>
    <button type="button" id="copy-code">
      复制代码
    </button>
    <a href="https://godbolt.org/" target="_blank" rel="noreferrer">
      打开 Compiler Explorer ↗
    </a>
  </div>
  <p class="source-note">
    点击运行会将当前代码发送到 godbolt.org。编译参数：-std=c++20 -O2 -Wall -Wextra
    -pthread；远程服务可能限流。也可复制代码到本地编译。
  </p>
  <pre id="code-output" aria-live="polite">尚未运行。修改代码后点击运行，结果显示在这里。</pre>
  <script src="../scripts/code-lab.ts"></script>
</section>
